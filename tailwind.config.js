/** @type {import('tailwindcss').Config} */
// here we are exporting the modules so we are entering the directory Here
module.exports = {
  content: ["./dist/*.html"],
  theme: {
    extend: {},
  },
  plugins: [],
}

